<h1>id:<%= @task.id %>のタスク修正</h1>

<p>修正前:<%= @task.content %></p>
<%= form_with(model: @task, local: true) do |f| %>
  <%= f.label :content, '訂正後' %>
  <%= f.text_field :content %>

  <%= f.submit '登録' %>
<% end %>

<%= link_to "タスク一覧に戻る", tasks_path %>